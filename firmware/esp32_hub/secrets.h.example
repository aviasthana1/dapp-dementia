#pragma once
// Copy to secrets.h (gitignored) and fill from Firebase Console → Project settings.

#define FIREBASE_PROJECT_ID "your-project-id"
#define FIREBASE_API_KEY "your-web-api-key"

// Optional: local time for timeLocal field (seconds from UTC). US Eastern example:
// #define TIMEZONE_OFFSET_SEC (-18000)
// #define TIMEZONE_DST_SEC (3600)
